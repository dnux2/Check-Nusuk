## Packages
framer-motion | Page transitions and advanced animations
recharts | Dashboard analytics and crowd metrics visualization
date-fns | Date formatting for alerts and tracking events

## Notes
- Tailwind config assumes extending colors with Nusuk-inspired theme (emerald green and gold)
- Map uses a highly polished CSS-grid interactive mock rather than a heavy WebGL library to ensure reliable zero-config rendering
- Camera feed uses CSS animations to simulate AI scanning
- Ensure `@shared/routes` and `@shared/schema` are available as defined
